/**
 * Stubbed builder DB client.
 * The workflow builder MVP uses Supabase for logging, so we keep this file minimal
 * to avoid pulling unused drizzle dependencies into the build.
 */

export const db = null as unknown;
export const migrationClient = null as unknown;
